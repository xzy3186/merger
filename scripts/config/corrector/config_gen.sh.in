#!bin/bash
WORKDIR=@MERGER_WORK_DIR@
CONFIGDIR=@MERGER_LIB_DIR@/../config
INPUT=${WORKDIR}/config/corrector/correction_config.yaml
OUTPUT=${WORKDIR}/config/corrector/config_${1}.yaml
MERGERLISTNAME=${WORKDIR}/mergerLists/mergerList${1}.txt

python ${CONFIGDIR}/config_gen.py -i ${INPUT} -o ${OUTPUT} -b AnamergerMain -k MergerListName -v ${MERGERLISTNAME}
